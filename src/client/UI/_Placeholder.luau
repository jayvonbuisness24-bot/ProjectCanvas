--!strict
--[[
	_Placeholder.luau (client/UI)

	Purpose:
		Reserved location for HUD, menus, and the future crosshair/UI
		customization systems. Empty by design —
		TASK-001_PROJECT_FOUNDATION.md excludes UI implementation from
		this task.

	Responsibilities:
		None yet.

	Public API:
		None. Not intended to be required.

	Dependencies:
		None.

	Future Notes:
		Customization (HUD layout, UI size, crosshair) must never create
		a competitive advantage, per CLAUDE.md's Player Ownership pillar.
		Configure defaults from shared/Config/CrosshairConfig.luau.
]]

return {}
