--!strict
--[[
	_Placeholder.luau (client/Viewmodels)

	Purpose:
		Reserved location for first-person viewmodel implementation
		(rigs, weapon presentation). Empty by design —
		TASK-001_PROJECT_FOUNDATION.md excludes viewmodel implementation
		from this task.

	Responsibilities:
		None yet.

	Public API:
		None. Not intended to be required.

	Dependencies:
		None.

	Future Notes:
		Per CLAUDE.md's Animation Rules, do not hardcode animation IDs
		here — store asset references in weapon/animation config modules
		once those exist.
]]

return {}
