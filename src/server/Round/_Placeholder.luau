--!strict
--[[
	_Placeholder.luau (server/Round)

	Purpose:
		Reserved location for round-state supporting modules used by the
		future RoundService (e.g. phase transition logic, scoring).
		Empty by design — no round gameplay exists yet.

	Responsibilities:
		None yet.

	Public API:
		None. Not intended to be required.

	Dependencies:
		None.

	Future Notes:
		Type with shared/Types/RoundTypes.RoundState and configure with
		shared/Config/RoundConfig.luau rather than hardcoding values.
]]

return {}
