--!strict
--[[
	_Placeholder.luau (server/Services)

	Purpose:
		Reserved location for top-level server Services (e.g.
		RoundService, WeaponService) that own gameplay authority. Empty
		by design — TASK-001_PROJECT_FOUNDATION.md excludes gameplay
		implementation from this task.

	Responsibilities:
		None yet.

	Public API:
		None. Not intended to be required.

	Dependencies:
		None.

	Future Notes:
		Each Service should have one clear responsibility (see
		docs/ENGINEERING_PRINCIPLES.md #2) and be named per CLAUDE.md's
		Naming Standards (e.g. RoundService, not "GameManager").
]]

return {}
